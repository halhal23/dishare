{
  "name": "dishare",
  "version": "1.0.0",
  "description": "dishare is Nuxt.js and Rails(API) Project",
  "author": "Hiroharu Hara",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "test": "jest",
    "build": "nuxt build",
    "build:stg": "cross-env NODE_ENV=\"staging\" nuxt build",
    "build:prod": "cross-env NODE_ENV=\"production\" nuxt build",
    "start": "nuxt build && nuxt start",
    "start:stg": "cross-env NODE_ENV=\"staging\" nuxt build && nuxt start",
    "start:prod": "cross-env NODE_ENV=\"production\" nuxt build && nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/proxy": "^1.3.3",
    "cross-env": "^7.0.2",
    "element-ui": "^2.4.11",
    "js-cookie": "^2.2.1",
    "nuxt": "^2.0.0",
    "vue2-google-maps": "^0.10.7",
    "vuex-persistedstate": "^3.0.1"
  },
  "devDependencies": {
    "@vue/test-utils": "^1.0.3",
    "jest": "^26.0.1",
    "lodash.clonedeep": "^4.5.0"
  }
}
